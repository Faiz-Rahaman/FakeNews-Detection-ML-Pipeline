# FakeNews-Detection-ML-Pipeline
Here’s a clean version under 350 characters:  > A robust ML pipeline for fake news detection using TF-IDF + LinearSVC and DistilBERT. Focuses on generalization, bias reduction, cross-domain evaluation, calibration, explainability (SHAP), and ensemble modeling to move beyond dataset-specific accuracy toward real-world robustness.
